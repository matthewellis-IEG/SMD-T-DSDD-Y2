<?php
// Create an array with random names
$names = ["Alice", "Bob", "Charlie", "Diana", "Eve"];

// Echo a single value (e.g., the second name)
echo $names[1] . "\n";

// Loop through the array and echo each name
foreach ($names as $name) {
    echo $name . "\n";
}
?>